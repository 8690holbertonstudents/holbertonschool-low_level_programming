#include "main.h"
/**
 * _strlen - use pointer
 * @s: input pointer from main
 * Return: len value to main
 */
int _strlen(char *s)
{
	int len;

	for (len = 0 ; s[len] != '\0' ; len++)
	{
	}
	return (len);
}
